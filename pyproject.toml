[project]
name = "com-newsreview-news"
version = "1.5.2"
description = "A Class Widgets 2 news reading plugin."
authors = [
    {name = "NewsReview"},
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
